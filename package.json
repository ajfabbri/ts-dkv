{
    "name": "ts-dkv",
    "version": "0.0.1",
    "description": "Toy app that builds a distributed key-value store (DKV) on top of ditto.live",
    "main": "main.ts",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "tsc"
    },
    "dependencies": {
        "@dittolive/ditto": "^4.4.2",
        "minimist": ">=1.2.6"
    },
    "devDependencies": {
        "@types/node": "^20.8.4",
        "tsc": "^2.0.4",
        "typescript": "^5.2.2"
    },
    "prettier": {
        "singleQuote": true,
        "trailingComma": "all",
        "printWidth": 80,
        "tabWidth": 4
    }
}
